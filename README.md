# 📈 Анализ динамики валют по данным ЦБ РФ

Сервис для периодического запроса курсов валют с API Центробанка РФ (www.cbr.ru), сохранения данных в PostgreSQL и логирования ошибок подключения. Проект полностью контейнеризирован — используется Docker Compose.

---

## 📌 Описание

Сервис выполняет следующие функции:

1. **Раз в 5 минут** запрашивает курсы валют через публичный API (по умолчанию — www.cbr.ru)
2. **Сохраняет данные** в PostgreSQL в две связанные таблицы (`requests` и `responses`)
3. **Логирует ошибки** подключения и таймауты в отдельный файл (`logs/error.log`)
4. **Работает через Docker** — весь стек (Python + PostgreSQL) запускается одной командой

```
exchange-rates-service/
 ├── docker-compose.yml # Конфиг Docker сервисов
 ├── app
 |   ├── Dockerfile # Сборка Python-приложения
 |   ├── requirements.txt # Зависимости Python 
 |   ├── config.py # Загрузка конфигурации 
 |   ├── database.py # Менеджер БД и таблиц 
 |   └── main.py # Основной сервис 
 ├── .env # Переменные окружения 
 ├── README.md # Документация 
 └── logs/
     └── error.log # Файл логирования ошибок
```

## 🚀 Быстрый старт

### Шаг 1️⃣ Клонирование и подготовка

```
# Клонируем репозиторий
git clone https://github.com/levitus-Kt/value_currency_postgre.git

cd value_currency_postgre

# Создаём папку для логов
mkdir -p logs
```

### Шаг 2️⃣ Проверка .env файла

Создайте и заполните .env (пример):

```
DB_HOST=db
DB_PORT=5432
DB_USER=db_user
DB_PASSWORD=your_secure_password_here
DB_NAME=exchange_rates_db

FETCH_INTERVAL=5                # интервал в минутах
API_TIMEOUT=10                  # таймаут запроса в секундах
API_URL=https://www.cbr.ru/scripts/XML_daily.asp
LOG_FILE=logs/error.log
```

### Шаг 3️⃣ Запуск через Docker Compose

```
# Сборка и запуск контейнеров
docker-compose up --build -d

# Проверка статуса
docker-compose ps
```

### Шаг 4️⃣ Просмотр логов

```
# Логи приложения в реальном времени
docker logs exchange_rates_app -f

# Логи ошибок (из файла)
tail -f logs/error.log

# Логи БД
docker logs exchange_rates_db -f
```

### Шаг 5️⃣ Остановка сервиса

```
# Остановить контейнеры и удалить их
docker-compose down

# Остановить, но сохранить контейнеры
docker-compose stop

# Возобновить работу
docker-compose start
```

## SQL

Ниже представлен SQL-запрос с JOIN для выгрузки истории запросов и полученных данных

```
SELECT 
    req.id,
    req.request_time,
    req.status,
    res.currency_code,
    res.currency_name,
    res.rate,
    res.nominal
FROM requests req
LEFT JOIN responses res ON res.request_id = req.id
ORDER BY req.request_time DESC, res.currency_code;
```
